AGENTS.md通用抽象版｜AI 开发协作总规则
本文件是项目的 AI 开发协作总规则。任何 AI、任何开发会话、任何功能迭代，在动代码或动文档前都必须先读本文件。

1. 最高优先级规则
开发前先想清楚，不要边猜边改。
必须先说明：
•当前假设
•本次目标
•验收标准
不确定就先查：
•真实代码
•真实文档
•真实命令输出
•真实页面表现
•真实业务结果
仍不确定，再问用户。
不允许猜：
•商业规则
•用户权限
•资源消耗规则
•交易 / 支付规则
•业务状态
•外部服务行为
•部署规则
每次只做：
•一个小功能
•一个小修复
•一个明确文档任务
只改和本次任务直接相关的文件。
任何改动都必须能追溯到用户需求。
不能为了“顺手”：
•重构
•优化
•改样式
•改文案
•改目录
•清理文件
•新增功能
•扩大范围
不能回滚用户或协作者已经做过的改动，除非用户明确要求。
完成前必须验证，不能只靠感觉说完成。

1.1 文档语言规则
所有面向用户、协作者、后续 AI 的项目文档，默认使用用户能看懂的语言。
执行规则：
•不要新建用户看不懂的项目文档，除非用户明确要求。
•旧文档如果被重新启用、同步、修订或作为当前开发指引使用，必须先改成用户能看懂的版本。
•命令、文件名、环境变量、模型名、API 字段名可以保留原词，但必须配解释。
•最终回复和项目管理说明默认使用用户能看懂的话。
•如果必须保留专业术语，要说明它在项目里的作用。

1.2 用户沟通规则
用户是产品所有者，不一定有工程背景。
回复时先给可落地结论，再解释原因。
涉及以下内容时，要先讲清：
•风险
•前置条件
•验证方式
•对产品的影响
•对用户的影响
•对成本的影响
•对上线的影响
高风险内容包括：
•钱
•权限
•资源消耗
•用户数据
•外部服务调用
•真实环境操作
•部署上线
•敏感信息
解释项目问题时，要落到：
•真实文件
•真实页面
•真实命令
•真实数据
•真实业务结果
避免只说抽象技术名词。
当技术细节影响产品决策时，必须讲清楚：
•现在能做什么
•还缺什么
•做错会有什么后果
•下一步最稳妥怎么做
所有面向用户的回复，必须默认使用非技术小白能听懂的话。
表达顺序：
1.结论：现在能不能用 / 有没有问题 / 要不要修
2.影响：对产品、用户、成本、上线有什么影响
3.动作：下一步最稳妥怎么做
4.证据：具体文件、命令、页面、数据或日志依据
不允许把程序员术语直接作为主要解释。
必须使用专业术语时，要先翻译成白话。
如果发现风险，不能只说技术现象，必须明确告诉用户：
•会不会误判结果
•会不会产生额外成本
•会不会影响用户使用
•会不会影响上线
•下一步先修什么
文件名、命令、字段名、模型名可以作为证据保留，但必须放在白话结论之后。

1.3 执行前强制对齐和计划归档闸门
没有完成以下三件事之前，AI 只能读取、搜索和分析，不能修改、删除、移动、格式化、重命名、提交或推送任何项目文件：
1.需求对齐
2.用户确认
3.计划归档
任何 AI 在修改代码、文档、配置、脚本、数据库、样式或其他项目文件前，必须先向用户说明：
本次目标
用户到底要解决什么问题。
当前假设
哪些事实已经确认，哪些仍是推断。
变更边界
预计只会查看和修改哪些文件、模块、页面或脚本。
禁止范围
明确说明本次不会顺手处理哪些相邻问题。
执行步骤
准备按什么顺序做。
验收标准
完成后用什么命令、页面现象、业务结果或 Git 状态证明完成。
风险点
是否涉及：
•钱
•权限
•资源消耗
•外部服务
•用户数据
•部署
•生产环境
•敏感信息
•不可逆操作
用户没有明确确认前，AI 不得开始修改文件。
用户在同一任务中明确说：
•开始
•执行
•确认
•按这个做
才算确认。
沉默、模糊语气或 AI 自己判断“应该可以”，都不算确认。
如果用户的要求很小，AI 也必须至少完成：
•目标
•范围
•步骤
•验收标准
不能因为任务小就跳过对齐。
确认后，AI 必须先把本次计划归档，再开始修改其他项目文件。
归档内容必须包含：
•目标
•变更范围
•执行步骤
•验收标准
•禁止范围
目的：
•防止长任务遗忘
•防止中断后丢上下文
•防止上下文压缩后跑偏
•方便后续 AI 接手
如果执行过程中用户调整目标，或 AI 发现必须扩大变更范围，必须先停止修改，更新计划和验收标准，重新取得用户确认后再继续。

1.4 只动相关文件硬规则
AI 每次只能修改和本次用户确认目标直接相关的内容。
每个被修改的文件，都必须能追溯到本次用户需求。
每一类改动，都必须能对应到执行前计划里的某一条。
不允许：
•顺手重构
•顺手优化
•顺手改样式
•顺手改文案
•顺手清理文件
•顺手统一格式
•顺手处理相邻问题
•顺手扩大功能范围
不允许因为“看起来更好”而改动未被用户要求的功能。
不允许把执行中发现的问题自动扩大成新任务。
如果执行中发现必须扩大范围，AI 必须立刻停止修改，说明：
•扩大范围的原因
•会影响哪些文件
•会带来什么风险
•为什么原计划不够
等用户重新确认后再继续。
如果发现工作区已有用户或协作者的改动，AI 必须默认保留，不得覆盖、回滚或格式化。
如果某个文件是否相关无法确认，默认不改，先查证或询问用户。

1.5 AI 执行计划归档要求
执行前计划必须归档到项目指定的执行计划目录。
文件名建议格式：
YYYY-MM-DD-简短任务名.md
归档文件必须使用用户能看懂的语言，并包含：
•用户原始需求摘要
•AI 对需求的理解
•当前假设
•计划修改范围
•明确不修改的范围
•执行步骤
•验收标准
•风险和需要用户确认的事项
•执行后的实际结果
•验证命令或验证方式
计划归档文件不得记录：
•API Key
•完整提示词正文
•用户隐私
•外部账号密码
•支付密钥
•数据库连接串
•内部密钥
•其他敏感信息

2. 每次开发必须先读本文件
每次开始以下任务前，AI 必须先读本文件：
•开发
•修 bug
•改文档
•删功能
•调配置
•改数据库
•改脚本
•改 UI
•改业务流程
•改部署相关内容
标准流程：
1.先读AGENTS.md
2.再读与本次任务相关的核心文档
3.再读真实代码或真实项目文件
4.再看 Git 状态
5.再做影响面扫描
6.再向用户说明目标、假设、变更边界、禁止范围、执行步骤、验收标准和风险点
7.等用户明确确认
8.先把执行计划归档
9.再开始修改项目文件
如果后续开发中发现某个步骤以后每次都必须做，必须把这个步骤补回AGENTS.md，不能只放在聊天记录里。

3. 当前权威来源顺序
当代码、文档、旧清单、聊天记录互相冲突时，按下面顺序判断：
1.真实代码和真实检查命令输出
2.当前版本核心文档
3.当前系统架构或项目总览文档
4.当前开发清单
5.其他专项文档
6.历史文档、快照文档、调试文档、聊天记录
系统架构或项目总览文档，只用于理解：
•系统结构
•模块边界
•核心链路
•技术栈
•本地运行口径
它不能覆盖真实代码、检查输出和当前核心文档。
涉及具体需求细节、数据结构、业务规则、流程规则、开发状态和验收命令时，仍以前面的权威顺序为准。
不要把历史资料、旧清单、快照文档、调试文档当成当前开发权威，除非用户明确指定。

4. Git 上传硬规则
每完成一个功能迭代，都必须走一次 Git 上传闭环。
“一个功能迭代”包括但不限于：
•新增功能
•修改功能
•删除功能
•修 bug
•调整 UI
•修改文案
•修改数据库结构
•修改初始化数据
•修改脚本
•修改配置
•修改外部服务接入
•修改核心业务流程
•修改权限相关逻辑
•修改后台相关逻辑
•修改部署相关逻辑
•修改开发文档
•修改项目管理文档
Git 上传闭环必须包括：
1.查看当前工作区
2.确认本次要提交的文件范围
3.只提交本次任务相关文件
4.运行相关检查命令
5.检查暂存区是否有格式或冲突问题
6.commit 信息必须包含短标题和详细中文正文
7.push 到远端分支
8.最终回复里说明 commit、push 结果和验证命令
commit 详细正文必须用用户能看懂的白话写清：
•本次改了什么文件或功能
•为什么要改
•解决了什么问题
•具体行为、规则或页面有什么变化
•跑过哪些验证命令
•结果是什么
•还有什么没完成、风险、阻塞或后续要注意的点
•如果没有，也要写“暂无已知遗留问题”
commit 可以保留一行英文短标题，但必须补充用户能看懂的中文详细正文。
如果因为以下原因不能 commit / push，不能把任务说成完整完成：
•用户明确要求暂不提交
•权限限制
•测试失败
•工作区冲突
•文件范围未确认
•暂存区混入无关内容
•远端推送失败
必须明确写出：
•未上传 Git 的原因
•下一步需要用户确认什么

5. 相关文件同步更新硬规则
任何新增、修改、删除、迭代，都必须同步更新所有相关文件，并标注进度。
相关文件包括但不限于：
•核心需求文档
•页面状态文档
•数据结构文档
•核心流程文档
•商业规则文档
•权限规则文档
•开发路线图
•开发清单
•AGENTS.md
•README
•部署文档
•运行手册
•脚本说明文档
•测试说明文档
•后台说明文档
•API 说明文档
同步更新时必须写清：
•当前状态
•本次改动影响了什么
•哪些功能仍未完成
•用什么命令或方式验证过
当前状态必须优先写中文：
•已完成
•部分完成
•待开发
•被外部条件卡住
•后续阶段
•第一版不做
英文缩写只作为辅助，不得作为主要阅读标签。
如果是删除功能，必须写清：
•删除原因
•替代路径
•受影响文件
•回滚方式
不能只改代码不改文档。
不能只改文档不更新开发清单里的进度。

6. 真实环境操作规则
如果要做真实环境操作，必须先确认前置条件。
真实环境操作包括但不限于：
•真实外部服务调用
•真实费用产生
•真实支付
•真实上传
•真实用户数据操作
•真实生产环境操作
•真实部署
•真实数据库写入
•真实第三方平台请求
必须先确认：
•测试对象存在
•测试对象状态正常
•测试对象具备对应权限
•资源额度足够
•已有必要输入素材或数据
•用户明确同意触发真实外部服务
•失败后有明确处理方案
只有用户明确同意时，才允许触发真实成本、真实支付、真实上传或真实生产操作。

7. 当前项目事实记录规则
AGENTS.md可以记录当前项目事实，但必须只记录已经被真实代码、真实命令或真实业务结果验证过的事实。
项目事实记录应包括：
•当前项目是否仍是原型，还是已经有真实业务链路
•当前已完成哪些核心能力
•当前哪些能力只是部分完成
•当前哪些能力尚未完成
•当前使用的主要技术栈
•当前运行方式
•当前存储方式
•当前部署状态
•当前外部服务接入状态
•当前后台能力
•当前真实链路是否已经完整验收
记录项目事实时必须避免：
•把一次小规模成功当成完整验收
•把本地可用当成线上可用
•把部分完成写成已完成
•把计划中的能力写成已实现
•把外部条件未满足的功能写成可交付

8. 业务红线
涉及业务规则时，不允许 AI 自己猜。
业务红线包括但不限于：
•价格规则
•权益规则
•续费规则
•升级规则
•退款规则
•资源消耗规则
•失败处理规则
•权限开通规则
•用户可见范围
•交付结果规则
•外部服务调用边界
•部署策略
凡是用户没有明确确认、文档没有明确记录、代码没有真实实现的业务规则，都不能擅自补全。
前端只展示规则，不能决定核心业务规则。
核心业务规则必须以后端或服务端可信逻辑为准。
敏感信息绝不能进入：
•前端
•日志
•报错
•下载文件
•公开文档
•Git 仓库
敏感信息包括但不限于：
•API Key
•完整提示词正文
•支付密钥
•数据库连接串
•用户隐私
•外部账号密码
•内部配置细节
如果流程涉及失败处理，必须明确：
•什么情况算平台失败
•什么情况需要退回资源
•什么情况不退回资源
•什么情况需要人工判断
•用户看到什么提示
•后台如何留痕

8.1 架构和实现红线
复杂环节和高准确性环节，必须优先使用更稳定、更可维护、更可验证的实现方式。
包括但不限于：
•上下文管理
•流程编排
•输出解析
•交易 / 支付
•资源消耗
•数据库
•外部服务接入
•错误处理
•结算
•验收脚本
新增或重构这些区域时，必须优先选择更适合长期维护的实现方式。
如果确实不能迁移或不能使用推荐方式，必须在相关文档和开发清单里写明：
•原因
•风险
•后续迁移条件
普通 UI、展示页或已有稳定文件，可以继续沿用周边风格。
不要为了“全项目统一”一次性重写无关代码。
注释优先用于解释复杂逻辑，不写空泛注释。
导入、命名、目录结构优先沿用项目现有规则，不新建一套风格。
数据库、认证、权限、任务状态、成本日志、输出记录等核心入口，必须保持单一可信入口，不能多处各写一套。
日志只能记录安全摘要，不能保存或输出完整用户需求正文、完整提示词正文、API Key 或其他敏感信息。

8.2 未来多模块扩展架构红线
如果项目未来会继续扩展多个模块、多个工具或多个业务入口，必须按“平台化架构”设计。
不能每新增一个模块，就复制一套独立的：
•支付逻辑
•权限逻辑
•资源消耗逻辑
•任务流程
•后台逻辑
•历史记录
•结算逻辑
•日志逻辑
新模块必须先进入统一注册体系，明确：
•模块 key / slug
•中文名称
•状态
•排序
•入口
•所需输入
•是否可购买
•是否运行任务
•是否消耗资源
•处理器类型
•输出结构
付费逻辑必须尽量复用统一商品和权益模型。
权限判断必须逐步从写死单个模块，过渡到按模块列表返回。
资源消耗逻辑必须逐步从单模块专用常量，过渡到后端统一计算规则。
任务运行必须拆成：
•通用任务生命周期
•模块专属处理器
通用层负责：
•登录
•权限
•输入归属
•幂等
•资源冻结
•状态
•历史
•后台日志
•结算
•退回
模块处理器只负责：
•输入校验
•处理方式选择
•外部 API / 本地处理
•输出解析
•结果适配
不调用外部 API 的模块，也要纳入同一套架构判断：
•如果只是展示型 / 模板型 / 课程型模块，可以不创建任务、不扣资源。
•如果有生成、导出、历史或计费结果，即使只做本地处理，也应复用任务、历史、资源消耗和后台日志规则。
前端工作台必须逐步抽出通用状态壳：
•开发中
•游客
•已登录无权限
•权限有效
•权限过期
•资源不足
•运行中
•成功
•失败
•历史恢复
每个模块只替换自己的表单、输入要求和输出组件。
后台必须逐步从单模块操作，过渡到可选择模块的统一管理入口。
任何新模块开放购买或运行前，必须补齐：
•核心文档
•开发清单
•数据结构
•权限返回
•页面入口
•后台入口
•资源消耗规则
•任务或非任务处理器
•历史 / 详情 / 成本说明
•检查脚本
•Git 上传闭环

9. 常用代码入口规则
项目应维护一份“常用代码入口索引”。
用于告诉 AI：
•主界面在哪里
•用户状态在哪里
•权限判断在哪里
•核心业务流程在哪里
•上传入口在哪里
•资产读取在哪里
•外部服务接入在哪里
•输出解析在哪里
•错误处理在哪里
•成本记录在哪里
•认证逻辑在哪里
•数据库模型在哪里
•后台入口在哪里
•历史记录在哪里
每个入口必须写清：
•文件路径
•负责什么
•什么情况下需要看
•是否是当前权威入口
•是否只是历史入口或候选入口
如果主链路发生变化，必须同步更新这份入口索引。

10. 开发流程
开发前
必须：
•读AGENTS.md
•说明本次目标和假设
•找到相关核心文档
•读真实代码
•查看 Git 状态
•做影响面扫描
•写出最小验收标准
开发中
必须：
•按现有风格和结构改
•小步修改
•不做无关重构
•不改无关文件
•不静默降低输出质量
•不随意改业务规则
•不暴露敏感信息
开发后
必须：
•跑相关检查
•更新相关文档和开发清单状态
•如产生未来必须步骤，更新AGENTS.md
•查看代码差异
•完成 Git 上传闭环
•最终说明：
•改了什么
•验证了什么
•上传了什么
•还有什么没完成

11. 常用检查命令规则
项目应维护一组常用检查命令。
检查命令应区分：
•本地启动检查
•数据库检查
•登录检查
•权限检查
•后台检查
•核心流程检查
•外部服务检查
•成本检查
•支付检查
•构建检查
•文档检查
运行检查命令前，必须先判断该命令是否会触发：
•真实外部服务
•真实费用
•真实支付
•真实上传
•真实生产数据写入
•敏感信息输出
凡是可能触发真实成本、真实支付、真实上传或生产环境操作的命令，必须先得到用户明确同意。
本地启动时，优先走快速启动路径：
1.先确认服务是否已经在运行
2.如果已经正常，不要重复启动
3.如果未启动，先做最小依赖检查
4.只有最小证据异常，才进入深度排查
遇到启动或外部服务调用失败时，不要一上来就大改代码。
先验证：
•服务是否真的启动
•端口是否可用
•数据库是否连接
•网络权限是否正常
•外部服务是否可达
•日志是否有真实报错
只有这些基础证据仍失败，才进入配置、代码或供应商排查。

12. 文档同步规则
改功能时必须同步相关文档。
常见对应关系：
改产品规则
同步：
•需求文档
•页面状态文档
•商业规则文档
•开发清单
•AGENTS
改页面状态
同步：
•页面状态文档
•开发清单
•必要时同步需求文档
改数据结构
同步：
•数据结构文档
•开发清单
•必要时同步相关业务文档
改核心业务流程
同步：
•核心流程文档
•商业规则文档
•数据结构文档
•开发清单
改交易 / 资源 / 权益规则
同步：
•商业规则文档
•页面状态文档
•数据结构文档
•开发清单
改后台
同步：
•需求文档
•页面状态文档
•商业规则文档
•开发清单
改部署
同步：
•路线图
•部署文档
•开发清单
改测试对象或验证命令
同步：
•AGENTS
•开发清单
•相关运行手册
改上线节奏、阶段里程碑、关键决策、真实跑通证据或工时复盘时，才同步上线作战台或项目总览文档。
项目总览文档不能替代：
•Git 提交
•开发清单状态更新
•核心文档同步
项目总览文档必须面向非技术小白也能读懂。
不能只把 Markdown 或清单原文追加进去。
必须从产品经理和用户体验视角重塑信息架构。
页面状态必须优先显示中文：
•已完成
•部分完成
•待开发
•被外部条件卡住
•后续阶段
•第一版不做
历史审计文件只记录当时证据，不要随便改写旧证据。
需要补充时，新增“后续更新说明”。

13. Git 和多人协作安全
每次开发前必须看 Git 状态。
不回滚自己没做的改动。
不清理无关未跟踪文件。
不把 debug、截图、日志、临时文件混进提交。
提交前必须看暂存文件列表。
commit 必须只包含本次功能相关文件。
push 前必须确认检查通过。
多个 AI 窗口并行开发时，建议：
•一个任务一个独立工作区
•一个任务一个独立分支
•不同窗口不要改同一代码块
不确定某个改动是谁做的，就当作用户或协作者做的，不能覆盖。

14. 必须停下来问用户的情况
遇到以下情况必须停下来：
•真实代码和核心文档冲突，但本地证据不足
•需要改商业规则
•需要改权益规则
•需要改退款规则
•需要改资源消耗规则
•需要改外部服务计费策略
•需要真实外部服务调用或可能产生费用
•需要真实支付
•需要生产数据库操作
•需要部署
•需要域名、证书、密钥、对象存储等生产配置
•需要删除功能
•需要删除文件
•需要废弃旧文档
•需要回滚或覆盖用户 / 协作者已有改动
•Git 上传前发现暂存区混入无关内容
•检查失败但原因不能立刻定位
•执行过程中发现必须扩大范围
•涉及敏感信息
•涉及不可逆操作
